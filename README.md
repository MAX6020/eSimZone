# eSIMZone Telegram Bot
## Telegram бот для продажи eSIM карт различных стран с интеграцией платежной системы.

## 🚀 Функционал
### Основные возможности:
+ Поиск eSIM по странам с фильтрацией
+ Интеграция с каталогом на сайте esimzone.ru
+ Платежная система через ЮKassa
+ Личный кабинет пользователя
+ Система заказов с историей покупок
+ Поисковая система по названию страны

### Особенности:

+ Многоязычное меню (русский)
+ Поддержка WebApp для интеграции с сайтом
+ Безопасное хранение данных пользователей
+ Логирование всех операций
+ Простая масштабируемая архитектура

## 🛠 Технологии
+ Python 3.8+
+ aiogram 3.x - асинхронный фреймворк для Telegram Bot API
+ SQLite3 - база данных (можно заменить на PostgreSQL)
+ YooKassa API - платежная система
+ Asyncio - асинхронное программирование
+ Pydantic (опционально) - валидация данных

## 📦 Установка и настройка
### 1. Клонирование репозитория
```py
git clone https://github.com/yourusername/esimzone-bot.git
cd esimzone-bot
```
### 2. Установка зависимостей
```py
pip install aiogram yookassa
```
### 3. Настройка конфигурации
```py
BOT_TOKEN=8588036832:AAH17iTX500TU1EL6h3AU8em9W4va9-FRxo
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
ADMIN_ID=ваш_telegram_id
```
### 4. Инициализация базы данных

База данных инициализируется автоматически при первом запуске.

### 5. Запуск бота
```py
python bot.py
```

## 📁 Структура проекта
```
esimzone-bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости
├── .env               # Конфигурационные переменные
├── esim_bot.db        # База данных SQLite
├── static/            # Статические файлы
│   ├── images/        # Изображения
│   └── qr_codes/      # QR-коды eSIM
├── keyboards/         # Клавиатуры (опционально)
├── handlers/          # Обработчики (опционально)
└── README.md          # Документация
```

## 🗄️ База данных

### Структура таблиц:
1. Пользователи (users)
```sql
user_id INTEGER PRIMARY KEY,
username TEXT,
first_name TEXT,
last_name TEXT,
registered_at TIMESTAMP
```
2. eSIM страны (esim_countries)
```sql
country_id INTEGER PRIMARY KEY AUTOINCREMENT,
country_name TEXT,
country_code TEXT,
price REAL,
data_amount TEXT,
validity_days INTEGER,
description TEXT
```
3. Заказы (orders)
```sql
order_id TEXT PRIMARY KEY,
user_id INTEGER,
esim_country TEXT,
esim_price REAL,
payment_id TEXT,
status TEXT,
created_at TIMESTAMP
```
## 💳 Платежная система
### Интеграция с ЮKassa:
+ Зарегистрируйтесь на kassa.yandex.ru
+ Создайте магазин и получите:
  + shop_id
  + secret_key
+ Настройте вебхуки для получения уведомлений
+ Добавьте данные в .env файл

### Процесс оплаты:
+ Пользователь выбирает eSIM
+ Создается платеж в ЮKassa
+ Пользователь переходит по ссылке для оплаты
+ После оплаты отправляется активационный код

## 🤖 Команды бота
### Основные команды:
+ /start - Запуск бота и главное меню
+ /catalog - Открыть каталог на сайте
+ /help - Получить помощь (если настроено)

### Меню бота:
+ 🌍 Поиск eSIM - Поиск по странам, цене, объему данных
+ 🛒 Мои покупки - История заказов
+ 📱 Каталог на сайте - WebApp с полным каталогом
+ ❓ Помощь - Инструкции и поддержка
### 🌐 WebApp интеграция
Бот интегрирован с сайтом через Telegram WebApp:
+ URL: https://esimzone.ru/catalog/
+ Поддерживает мобильные устройства
+ Прямой переход к покупкам

## 🚀 Развертывание
### На локальном сервере:
```bash
# Установка как службы (Linux)
sudo cp esimzone-bot.service /etc/systemd/system/
sudo systemctl enable esimzone-bot
sudo systemctl start esimzone-bot
```
### На облачных платформах:
```bash
heroku create esimzone-bot
heroku config:set BOT_TOKEN=ваш_токен
git push heroku main
```
2. PythonAnywhere
 + Загрузите файлы через веб-интерфейс
 + Настройте задание по расписанию для бота
3. VPS (Ubuntu)
```bash
# Установка зависимостей
sudo apt update
sudo apt install python3-pip python3-venv

# Настройка бота
mkdir ~/esimzone-bot
cd ~/esimzone-bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Запуск через screen или tmux
screen -S esimzone-bot python bot.py
```
## 🔧 Расширение функционала
### Добавление новых стран:
Добавьте данные в таблицу esim_countries через SQL:
```sql
INSERT INTO esim_countries (country_name, country_code, price, data_amount, validity_days, description)
VALUES ('Франция', 'FR', 8.99, '2GB', 10, 'eSIM для Франции, 2GB на 10 дней');
```
### Интеграция с другими платежными системами:
+ Создайте новый модуль в payment_processors/
+ Реализуйте интерфейс для платежной системы
+ Обновите обработчик покупок
### Добавление административных функций:
```py
# Пример админ-команды
@dp.message(Command("admin"))
async def admin_panel(message: types.Message):
    if message.from_user.id == ADMIN_ID:
        # Показать админ-панель
        pass
```

## 📊 Мониторинг и логирование
Бот включает встроенное логирование:
+ Уровень: INFO для основных событий
+ ERROR для критических ошибок
+ Логи хранятся в консоли и могут быть перенаправлены в файл
## 🛡️ Безопасность
### Рекомендации:
+ Никогда не коммитьте секретные ключи в Git
+ Используйте переменные окружения
+ Регулярно обновляйте зависимости
+ Настройте брандмауэр для ограничения доступа
+ Используйте HTTPS для всех внешних запросов
### 📞 Поддержка
+ Telegram: @your_support_username
+ Email: support@esimzone.ru
+ Сайт: https://esimzone.ru
